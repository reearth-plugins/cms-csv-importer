id: cms-csv-importer
name: CMS CSV Importer
version: 1.0.0
extensions:
  - id: csvImporter
    type: widget
    name: CSV Importer
    schema:
      groups:
        - id: appearance
          title: Appearance
          fields:
            - id: primary_color
              title: Primary color
              type: string
              ui: color
